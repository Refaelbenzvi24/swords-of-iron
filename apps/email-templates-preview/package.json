{
  "name":            "@acme/email-templates-preview",
  "version":         "1.0.0",
  "description":     "",
  "scripts":         {
    "dev":        "email dev --port=3001",
    "clean":      "rm -rf .turbo node_modules",
    "lint":       "eslint .",
    "lint:fix":   "pnpm lint --fix",
    "type-check": "tsc --noEmit"
  },
  "keywords":        [],
  "author":          "",
  "dependencies":    {
    "@acme/email-templates": "*",
    "react-email":           "1.8.0-canary.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
